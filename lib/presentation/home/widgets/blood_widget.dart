import 'package:flutter/cupertino.dart';

class BloodWidget extends StatelessWidget {
  const BloodWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Blood screen is working'),
    );
  }
}
