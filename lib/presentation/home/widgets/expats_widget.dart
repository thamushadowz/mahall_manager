import 'package:flutter/cupertino.dart';

class ExpatsWidget extends StatelessWidget {
  const ExpatsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Expats screen is working'),
    );
    ;
  }
}
